
const multiply = (a, b) =>a * b
console.log(multiply(2,5))

//Задача: определить, почему исходня функция не работает?
/**
 * const multiply = (a, b) =>{
 *     a * b
 * }
 */
//Решение: функции не хватало return, или записи в одну строку. Последнее говорит о неявном возврате результата функции.

